name: GitGoogleContainer
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      - name: Login in Docker Registry
        uses: docker/login-action@v1
        with:
          registry: registry.sioechoy.com
          username: xiafei
          password: 123
      - name: Docker pull and push
        run: |
          docker pull k8s.gcr.io/ingress-nginx/controller:v0.35.0  
          docker tag  k8s.gcr.io/ingress-nginx/controller:v0.35.0  registry.sioechoy.com/xia-fei/ingress-nginx/controller:v0.35.0       
          docker push registry.sioechoy.com/xia-fei/ingress-nginx/controller:v0.35.0
